[package]
name = "server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
glsl-parser = { path = "../glsl-parser" }
tokio = { version = "1.17.0", features = ["full"] }
tower-lsp = "0.17.0"
